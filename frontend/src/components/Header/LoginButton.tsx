import { IconButton } from '@/components/util/IconButton';

export const LoginButton = () => (
  <IconButton href="/login" variant="outline-primary" class="mr-1" icon="material-symbols:login" >
    Login
  </IconButton>
);
